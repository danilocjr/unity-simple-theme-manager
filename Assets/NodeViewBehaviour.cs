using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class NodeViewBehaviour : MonoBehaviour
{
    //[SerializeField] private GameObject[] offAtStart;

    //[Header("Text Theme")]
    //[SerializeField] private ThemeColorSchema _textTheme = null;
    //[SerializeField] private TMP_Text[] defaultTexts;
    //[SerializeField] private TMP_Text[] errosTexts;

    //[Header("Button Theme")]
    //[SerializeField] private ButtonColors _buttonTheme = null;
    //[SerializeField] private Button[] buttons;

    //public void SetInitialState(ThemeColorSchema textTheme = null, ButtonColors buttonTheme = null)
    //{
    //    if (textTheme)
    //        _textTheme = textTheme;

    //    if (buttonTheme)
    //        _buttonTheme = buttonTheme;

    //    if(_textTheme && _buttonTheme)
    //        SetTheme();

    //    foreach (var o in offAtStart)
    //        o.SetActive(false);
    //}

    //private void SetTheme()
    //{
    //    foreach (var dt in defaultTexts)
    //        dt.color = _textTheme.lightText;

    //    foreach (var et in errosTexts)
    //        et.color = _textTheme.errorText;

    //    foreach (var bt in buttons)
    //        bt.colors = _buttonTheme.GetColorBlock();
    //}
}
